# Verb: heartbeat

**Closed list:** yes · **Alias of:** update

Same write path as `update` (Face narrative patch + Strip append). Preferred name when the intent is an attention tick.

W1: stub only. Implementation: W3.

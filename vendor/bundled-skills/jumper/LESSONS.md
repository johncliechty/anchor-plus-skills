# LESSONS — <skill-name>

> Curated, distilled lessons promoted from `journal/` by the sleep loop (P0.D). This
> file is WRITE-GATED: an entry lands here only after passing the North-Star gate and
> the eval (no-regression) gate. Do not edit by hand outside a sleep cycle.

_(no lessons promoted yet — a fresh skill repo starts empty; the sleep loop fills this
from genuine-execution journal entries.)_

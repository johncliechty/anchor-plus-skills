{
  "schema": "canary-set/v1",
  "skill": "skill-repo-template",
  "version": 1,
  "append_only": true,
  "note": "Wave 6 (P0.E / FIX-6) seed canary SET for the per-skill repo skeleton, recorded as a DATA/ref (the Foundry stays stateless prose — canaries are data, never runtime). Versioned ALONGSIDE the skill: a sleep cycle may PROPOSE a new version that APPENDS canaries, but may never edit or delete an existing one — a deletion is a human call (enforced by src/governance.mjs validateCanarySetChange). Each canary is deterministic and runnable by the wave-2 canaryEvalValidator (input -> expected). The exemplar skill is the 'review classifier' used by the wave-5 sleep-loop fixtures: input { kind, text } -> output { verdict }.",
  "canaries": [
    {
      "id": "c1",
      "kind": "capability",
      "name": "flag-unsupported-claim",
      "input": { "kind": "claim", "text": "42% with no source" },
      "expected": { "verdict": "flag-unsupported" }
    },
    {
      "id": "c2",
      "kind": "capability",
      "name": "pass-sourced-claim",
      "input": { "kind": "claim", "text": "42% (Smith 2026, table 3)" },
      "expected": { "verdict": "flag-unsupported" }
    },
    {
      "id": "ng1",
      "kind": "non-goal",
      "name": "style-rewrite-is-out-of-scope",
      "input": { "kind": "style", "text": "rewrite this in passive voice" },
      "expected": { "verdict": "out-of-scope" }
    }
  ]
}

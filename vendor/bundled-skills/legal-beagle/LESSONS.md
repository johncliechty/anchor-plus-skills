# LESSONS — legal-beagle (sleep-cycle output; created 2026-08-15)

1. **The anti-hallucination classes are charter criteria, not prose.**
   `bin/legal-round.mjs` cites them by journal id: [lb2] the 0001
   wrong-reporter-cite class, [lb3] the 0002 PLR class, [lb4] the 0004/0006
   overclaiming class (three refuters turned an absolute NO-GO into
   CONDITIONAL GO). A finding must name the criterion it blocks.
2. **Single-family seats must reset client trust every Heavy run.** 0006:
   prefs coding=review=grok ⇒ cross_model:false — and the run must SAY so,
   plus restate the engine-deleted/library-scope fact, so a client never
   carries stale confidence forward. (journal 0006)
3. **This engine already mechanizes Elegance rule 5** — one dry round ends
   the loop (`legal-round.mjs` dry break). It is the portfolio's cleanest
   per-round hook; new round machinery copies THIS shape.

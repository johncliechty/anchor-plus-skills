# Tidy-Idy GUI (v2) — Execution Log

Foreman appends wave-by-wave build records here. Build branch: `foreman/tidy-idy-gui`.
Plan: `IMPLEMENTATION-PLAN.md` (10 waves). Test gate: `node --test`.

(v1 tidy-idy build docs preserved as `*-v1.*` in this folder.)

# LESSONS — zombie-hunter (sleep-cycle output; created 2026-08-15)

1. **Liveness metrics must name their observed process set.** The burn ledger
   was Claude-JSONL-only while Grok sessions burned invisibly (0003) — the
   same blind spot as watching node.exe while seats spawn as claude.exe
   (jumper 0024). A leg per engine, or say which engines are dark.
2. **"Zombie" means NOT TIED TO A SESSION** — John's definition (0002): not
   under active VS Code / Anchor / terminal. A stale shell image alone never
   KEEPs a spender; an inactive host never SUPERVISES.
3. **An empty seed + a clean radar = report CLEAN and refuse action theater**
   (0004). Never invent a target to justify the sweep.
4. **The hunter had a silent tool death of its own** — "Crucible LITE
   silent-died post band stamp" (0003) sat unanalyzed in this journal while
   the skill hunted silent deaths elsewhere. The sleep loop must read its own
   journal first (the Ecgberht 0084 law, locally).

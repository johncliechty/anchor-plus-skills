# Ramanujan — North Star v3 (FULL 6-pillar) — ★ LOCKED by the user 2026-06-21 ★

> Supersedes v2. All THREE pillar research dives DONE + engine-verified converged: dive-1 (UNDERSTAND+firewall),
> dive-2 (SOLVE+VERIFY, decision c), dive-3 (DIALOGUE+FORMALIZE+CONTEXTUALIZE, 7 rounds). Band: FULL.
> **STATUS: LOCKED (user approved 2026-06-21). This is the immutable objective. Drift detection is now ACTIVE — every
> Stage-1/Stage-2 element must trace to an NS criterion; out-of-scope growth -> Grasscatcher, refinement -> a HALT-for-user
> North-Star amendment (never silent re-scope). Stage 0 COMPLETE; proceeding to Stage 1 (Master Plan).**

## OBJECTIVE
**Ramanujan is a mathematical reasoning PARTNER** — six composed pillars on ONE shared spine (a typed CLAIM ledger + the
VERIFY router + a within-session STATE model). Understand/Contextualize EMIT typed claims into the ledger;
Solve/Verify/Dialogue/Formalize are STATEFUL and read/write it. It is ONE tool, not six silos.
- **UNDERSTAND** — given a paper's METHOD, produce a rigorous laddered COMPREHENSION (computes/why/where-it-breaks) AND,
  when computable, the understand→build FIREWALL (a pure/offline/deterministic reference fn trusted by reading-INDEPENDENT checks).
- **SOLVE** — given a NEW problem, GENERATE a candidate via a stateless Polya/Schoenfeld pass wrapped by a stateful
  metacognitive CONTROL loop (continue/switch/ABANDON on a budget); generation is cheap + UNVERIFIED until the stack verifies it.
- **VERIFY** — an ALWAYS-ON verification ROUTER decomposes any solution into typed CLAIMS and routes each to the strongest
  APPLICABLE generator-INDEPENDENT verifier: Lean (formalizable proofs, OBSERVED) → cross-family (informal proofs,
  CORROBORATED) → firewall (computational, OBSERVED-computational); honest ABSTAIN/FLAG when none applies.
- **DIALOGUE** — conduct a stateful mathematical dialogue: a Lakatos conjecture→counterexample→refine loop, mixed-initiative
  ask-vs-assert, asserting-as-settled ONLY what the ledger marks VERIFIED (Grice Quality), with an anti-sycophancy STICKY ledger.
- **FORMALIZE** — help build/refine a formalization of an idea (a Lakatosian definition-forging loop: examples/monsters →
  monster-bar/concept-stretch/proof-generated → example-space stability), faithfulness checked by a CROSS-family/Lean/SMT
  roundtrip (never same-family), the human gate the convergence authority.
- **CONTEXTUALIZE** — situate an idea in human mathematical knowledge by COMMISSIONING researchPrime (prior-art) +
  Gandalf-SITUATE (structure-mapping + outside-view); native = math relation classification {generalization|specialization|
  equivalence|instance|structural-analogy}; every connection is a CONCEPTUAL claim routed to VERIFY (analogy never proves).

## THE HONESTY LAW (the load-bearing invariant across ALL six pillars; hardened over 7 adversarial rounds)
**No same-family-authored object may reach a VERIFIED rung.** The verifier is OUT-OF-MODEL (a deterministic execution
sandbox / cross-family model / Lean / SMT roundtrip). A same-family model may PROPOSE a counterexample or verification but
NEVER ADJUDICATE a status flip. The autonomous-only tier settles (VERIFIED) ONLY literal finite computations whose domain
is the literal execution trace; EVERYTHING formalization-dependent, unbounded, proof-bearing, or conceptual ABSTAINS to
CONJECTURAL and routes to the out-of-model certifier. Same-family critique (Gandalf advisory, in-process adversarial) is
ADVISORY-only (a NOTES field, never the rung). Enforced by machine canaries (independence, inverted-completeness,
coverage-stamp, transitions-invariant) + a gradeable oracle.

## NORTH-STAR CRITERIA (testable — drive the inclusion test + drift detector)
- NS1 verified comprehension (laddered; OBSERVED needs an executed/external match).
- NS2 generative SOLVE + an executable CONTROL state machine.
- NS3 always-on independent VERIFY router by claim type; trust ONLY from a generator-independent verifier; honest abstain.
- NS4 autoformalization FAITHFULNESS (every Lean/formal check gated by a cross-family/Lean/SMT roundtrip; no green proof of a wrong statement).
- NS5 honest per-claim STAMP (rung + verifier-family); cross-family lifts to CORROBORATED; kernel-checked = OBSERVED.
- NS6 stateful DIALOGUE that never asserts unverified math as settled (Grice Quality + the anti-sycophancy sticky ledger).
- NS7 interactive FORMALIZE with the human gate + example/monster regression as the convergence authority.
- NS8 CONTEXTUALIZE composes researchPrime/Gandalf-SITUATE WITHOUT reimplementing (machine-checkable commission boundary).
- NS9 THE HONESTY LAW: no same-family object reaches a VERIFIED rung (the four machine canaries) + a gradeable oracle.

## SCOPE
- **IN (buildable):** all six pillars on the shared spine + the four honesty canaries + the gradeable oracle + an explicit author-SKILL.md wave.
- **DEPENDENCIES (decision c's build cost — integration, not subscription fees):** cross-family MODEL access (a 2nd
  family) + a Lean toolchain (free/open-source: elan/lake + mathlib) + an SMT roundtrip certifier. ⇒ Ramanujan is a
  cross-family + Lean/SMT integration, NOT a pure-subscription autonomous build.
- **HALT-gated:** the cross-family/Lean/SMT certifiers THEMSELVES; autonomous silent intent-classification; the human gate.
- **OUT / reversed:** the dive-1 Oranges drop of formal proof is REVERSED (Lean/SMT in scope). A bespoke cross-session
  memory store is DROPPED (within-session state + the Phase-0 journal substrate; FLAGGED).

## INHERITS (compose, don't rebuild)
Phase-0 (handoff/journal/sleep) · Gandalf v1 (advisory diagnose + SITUATE + the commission pattern) · the dive-1/2/3
converged engines (UNDERSTAND+firewall, SOLVE+verify stack, the partner layer).

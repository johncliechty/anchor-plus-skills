# Ramanujan — CANDIDATE North Star (Stage 0; awaiting USER lock)

> Complexity band: **FULL** (high stakes + irreversibility — forced, not defaulted). Brownfield ingest of the
> ENGINE-VERIFIED researchPrime dive: the converged method is CONFIRMED; the R1–R7 residuals are the GAPs the plan resolves.
> Substrate: single-family (Claude) ⇒ **cross_model:false**. STATUS: NOT LOCKED — the user is the convergence authority.

## OBJECTIVE (the locked statement, once approved)
**Ramanujan is a math/algorithm UNDERSTANDING skill.** Given a paper's METHOD — delimited as the **method/algorithm
section + cited equations, EXCLUDING the authors' released code** — it produces:
- **(a) a rigorous, laddered VERIFIED COMPREHENSION:** what the method computes, why it works, and where it breaks,
  each claim placed on the OBSERVED > CORROBORATED > CLAIMED > UNVERIFIED ladder; and
- **(b) WHEN the method is computable, the understand→build FIREWALL:** a PURE, OFFLINE, DETERMINISTIC reference
  function + spec + unit tests that is a trustworthy oracle — trusted ONLY by checks INDEPENDENT of the agent's own
  reading — which downstream skills (data / ML-code / code-verify) bind their executed numbers to.

It **EMITS typed commission envelopes** to Gandalf (diagnose) and researchPrime (situate); **autonomous DISPATCH is
HALT-gated to Increment-2.** It does NOT re-run their logic inline (no-inline-reimplementation canary-enforced;
commission ATTRIBUTION honor-system this cycle). Single-family substrate ⇒ cross_model:false; commission/situate rung
capped at CLAIMED.

## NORTH-STAR CRITERIA (testable — the inclusion test + post-lock drift detector check these)
- **NS1 — Verified comprehension.** Emits (a) for every artifact kind, each claim laddered; OBSERVED requires an
  executed-ref-fn / external-anchor match (a same-agent re-derivation alone caps at CORROBORATED).
- **NS2 — Independent-trust firewall.** For a computable method, the reference fn is pure/offline/deterministic and its
  trust rests ONLY on reading-INDEPENDENT checks (dimensional + the paper's own quoted numbers + closed-form/limit/
  conservation anchors). The comprehension+ref-fn SHARED-ORIGIN PAIR's mutual agreement is NOT counted as evidence.
- **NS3 — Honest, machine-readable degradation.** Emits `firewall_status` (computable | not-applicable),
  `anchor_coverage` (full … dimensional-only), `reduced-warranty`, `warranty_excludes[]` — never silently asserts trust
  it cannot warrant (proof-only ⇒ firewall_status:not-applicable, never a fabricated oracle).
- **NS4 — Gradeable oracle.** Catches the must-catch planted-defect set (derivation / dimensional / off-by-one /
  convergence-stability / comprehension-narrative / firewall-inapplicable), each by ≥1 INDEPENDENT check; passes a SOUND
  set within a stated FP tolerance; and beats BOTH a fixed canned baseline AND an ABLATION arm by the margin G.
- **NS5 — Machine-checkable composition boundary.** EMITS typed Gandalf/researchPrime commission envelopes;
  no-inline-reimplementation is canary-enforced (B5-style); attribution honor-system; autonomous DISPATCH + the
  cross-family re-implementation UPGRADE are HALT-gated to Increment-2.
- **NS6 — Honest single-family stamps.** cross_model:false; commission/situate rung capped at CLAIMED; the 3-risk CBS
  register (same-model re-impl correlation; trust-checklist sufficiency; dimensional-only coverage) FLAGGED, not mitigated.

## SCOPE BOUNDARIES
- **IN — autonomously buildable (Foreman waves):** comprehension-protocol SKILL.md + the firewall-applicability
  classifier; the firewall builder + the honesty stamps; the typed commission-envelope EMITTERS; the no-inline-
  reimplementation boundary canary; the planted-defect oracle fixture SET + the discriminating scorer; journal
  integration; and an **explicit "author SKILL.md" wave with its own done-when** (the Gandalf-cycle lesson).
- **HALT-gated (Increment-2):** autonomous commission DISPATCH (orchestrator bridge + commission-id ledger);
  the cross-family re-implementation UPGRADE (same spawn dependency); the cross-family GROUNDED rung.
- **OUT — dropped, flagged:** formal theorem-prover proof verification (Oranges drop — breaks the stateless-SKILL.md
  constraint + the autonomous-build budget; the firewall's trust rests on independent EMPIRICAL checks instead).

## GAPS the plan must resolve (from the dive's residuals; not part of the lock)
R1 fixture roster + k/k' + FP tolerance (REQUIRED-before-build for the scorer wave) · R2 ref-fn language ·
R6 anchor-coverage threshold policy · R7 applicability-enum boundary (rate-bound vs proof-only).

## INHERITS (built + green — compose, don't rebuild)
Phase-0 infra (handoff contract, 7-field journal, sleep loop) · Gandalf v1 (the diagnose engine + the seam/schema
pattern to reuse for the commission envelopes).

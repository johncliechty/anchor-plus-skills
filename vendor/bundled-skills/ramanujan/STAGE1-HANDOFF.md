# Ramanujan Crucible — Stage 1 (Master Plan) HANDOFF

> Written 2026-06-21. Crucible Stage 0 COMPLETE: the full 6-pillar North Star is LOCKED (user-approved) at
> `NORTH-STAR-CANDIDATE-v3.md`. Drift detection is ACTIVE. This doc resumes the Crucible at Stage 1.

## Where we are
- **Stage 0 DONE.** Three engine-mode researchPrime dives converged + Judge-confirmed:
  - dive-1 UNDERSTAND+firewall → `planning\research\`
  - dive-2 SOLVE+VERIFY (decision c: cross-family + Lean + firewall, always-on) → `planning\research-solve-verify\`
  - dive-3 DIALOGUE+FORMALIZE+CONTEXTUALIZE partner layer (7 rounds) → `planning\research-partner-layer\`
- **North Star LOCKED:** 6 pillars (Understand/Solve/Verify/Dialogue/Formalize/Contextualize) on ONE shared spine
  (typed claim ledger + verify router + within-session state); the HONESTY LAW (no same-family object reaches a VERIFIED
  rung; verifier out-of-model; same-family proposes-never-adjudicates; autonomous tier = literal computations only;
  everything else ABSTAINS + routes to the c-promoted cross-family/Lean/SMT certifier); 4 machine canaries + a gradeable oracle.

## Stage 1 = Master Plan (the next Crucible step)
Per the Crucible protocol: Oranges brainstorm (assumption-map → premortem → ideate) → batch triage → a phased plan with
near-term specifics → the Shark-Tank loop to convergence (3 fresh-context Sharks through the trio tally gate, ≥2-agree
BLOCKER, the engine driver at `~/.claude/skills/researchPrime/run-ramanujan-round.mjs` reused for the Crucible Shark
rounds) → **USER approves the Master Plan**. Then Stage 2 (Implementation Plan → Foreman handoff).

## Carry into Stage 1 (the converged intakes — the AGENT-IMPLEMENTATION sections)
- `planning\research\DELIVERABLE.md` (Understand+firewall: comprehension protocol, firewall builder, applicability classifier).
- `planning\research-solve-verify\DELIVERABLE.md` (Solve+Verify: generation+CONTROL loop, the verification router, the
  claim-type dispatch, the firewall/cross-family/Lean/SMT verifier table, the tiered oracle).
- `planning\research-partner-layer\DELIVERABLE.md` (Dialogue/Formalize/Contextualize: the shared ledger, the flip law,
  the dialogue state machine, the Lakatosian formalize loop, the contextualize compose boundary, fixtures i-xiv).

## Mandatory plan inclusions (program lessons + dive residuals)
- An EXPLICIT "author SKILL.md" wave with its own done-when (the Gandalf-cycle lesson — never let Foreman build the
  engine+canary spine with no SKILL.md).
- The four HONESTY-LAW machine canaries as gated waves (independence, inverted-completeness, coverage-stamp, transitions-invariant).
- HALT-gated (decision c's build cost): the cross-family verifier + Lean toolchain + the SMT-roundtrip certifier;
  autonomous silent intent-classification; the human gate.
- REQUIRED-before-build residuals: R1 (solve oracle fixture roster + k/k' + FP tolerance) · P7 (the literal-execution
  recognizer = parse-only, domain literal present in the input AST) · P8 (the SMT-roundtrip faithfulness contract).
- Build sequencing honesty: the autonomous (subscription) waves vs the c-dependency (cross-family/Lean/SMT) integration waves.

## Discipline (unchanged)
Anti-drift from the LOCKED North Star (every wave traces to an NS criterion; out-of-scope → Grasscatcher, refinement →
HALT-for-user amendment); real engine-gated Shark Tanks (≥2-agree BLOCKER, convergence-until-dry); single-family honesty
stamps (cross_model:false for the planning loop itself); state on disk + memory.
